Android Media Player Project.
- Uses two applications.
- First application acts as a server.
- Second application acts as a client.

- User interacts through the client application interface to play music.
- Server application serves up the requests and contains an sqlLite database to keep track of actions.
